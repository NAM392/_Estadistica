#include <iostream>
#include<conio.h>
#include<iomanip>

int main()
{
   // clrscr();

    int NUM ;
    NUM = 25 ;

//    std::cout << std::setiosflags(std::showbase()) ;

    std::cout << "\n\n hexadecimal " << std::hex(NUM) ;
    std::cout << "\n\n decimal " << std::dec(NUM);
    std::cout << "\n\n octal " << std::oct(NUM);

    std::cout << "\n\n\n\n" ;

    getch();







}
